# SDD Config — movi-rutas-conductor-app
# Auto-generated by sdd-init on 2026-06-28
# Mode: hybrid (Engram + openspec files)

project:
  name: movi-rutas-conductor-app
  display: Movi Rutas Conductor
  stack: flutter
  version: 1.0.0+1
  sdk: ^3.10.3 (Dart 3.12.0 / Flutter 3.44.0)
  fvm: false  # fvm not configured; flutter 3.44.0 installed globally

architecture:
  pattern: feature-based clean-ish
  state_management: flutter_bloc ^9.1.1 + equatable ^2.0.7
  auth: Keycloak OAuth2 (password.toUpperCase transformation)
  persistence: SharedPreferences (auth), InMemory (routes — placeholder)
  websocket: STOMP via stomp_dart_client, queue-based buffering, 10s heartbeat
  background: flutter_foreground_task + workmanager, separate isolate
  location: geolocator ^14.0.2, dual-source with 500ms dedup
  structure:
    - lib/features/{auth,route_tracking}/{bloc,models,pages,repositories,services}/
    - lib/core/{constants,errors,services}/
    - docs/estandares/ (10 files: testing, bloc, estilo, etc.)
    - ai/ (2 agent prompt templates)

testing:
  runner: flutter test
  frameworks:
    - flutter_test (sdk)
  mocking: none
  coverage: false  # no coverage tooling configured; /coverage/ in .gitignore
  integration: none
  e2e: none
  layers:
    unit: false
    widget: false  # test/widget_test.dart is a TODO placeholder
    integration: false
  strict_tdd: false
  strict_tdd_reason: "No real tests exist yet. Only a placeholder test/widget_test.dart with a TODO comment. Activating strict TDD would block all code changes until tests are written. Recommended: establish test patterns first, then adopt TDD incrementally."

conventions:
  code_language: english
  ui_language: spanish
  identifiers: english
  const_preferred: true
  final_over_var: true
  no_dynamic_in_models: true
  no_setstate_for_shared_state: true
  no_utils_helpers_common: true
  repository_pattern: true
  imports_order: "dart: → package: → project (blank line separators)"
  lint_set: flutter_lints recommended (no custom rules)
  file_naming: snake_case
  class_naming: PascalCase
  method_naming: camelCase

phase_rules:
  init:
    - context_detection: inspect project files and conventions
    - testing_capabilities: detect runner, layers, coverage
    - persistence: save to Engram and/or openspec/ based on mode
    - registry: build .atl/skill-registry.md
  propose:
    - must_save: sdd-propose/{project}/{change-key}
  spec:
    - format: gherkin-light (Given/When/Then scenarios)
    - must_cover: happy_path, error_path, edge_cases
  design:
    - must_include: file_list, dependency_graph, testing_approach
  tasks:
    - must_break_down_by: test, impl, verify per unit
  apply:
    - test_first_if_strict_tdd
  verify:
    - must_execute: all test scenarios from spec
    - must_report: pass/fail per scenario
  archive:
    - must_sync: delta specs, confirm no drift

changes:
  archived:
    - name: routepoint-mapping-standardization
      date: 2026-06-28
      status: completed
      specs:
        - domain: mapping
          action: created
      tasks_total: 9
      tasks_completed: 8
      tasks_exception: "1 pending (task 3.2 — STOMP backend tolerance of altitude:null, requires real environment). verify-report: PASS, user explicitly authorized archive."
      verify_status: PASSED
    - name: quick-wins-refactoring
      date: 2026-06-28
      status: completed
      specs:
        - domain: refactoring
          action: created
      tasks_total: 11
      tasks_completed: 10
      tasks_exception: "1 pending (task 4.2 smoke test — manual, requires physical device). reconcile: verify-report passed all specs, user explicitly authorized archive."
      verify_status: PASSED
    - name: config-extraction
      date: 2026-06-28
      status: completed
      specs:
        - domain: env-config
          action: updated
      tasks_total: 13
      tasks_completed: 13
      tasks_exception: null
      verify_status: PASSED
