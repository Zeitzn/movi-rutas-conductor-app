# Keycloak client secret for the mobile app
# ⚠️ This is intentionally stored in the APK (see PKCE caveat in EnvConfig)
AUTH_CLIENT_SECRET=<keycloak-client-secret>

# Profiles API base URL — used to fetch driver profile after login
PROFILES_API_BASE_URL=http://localhost:8080
